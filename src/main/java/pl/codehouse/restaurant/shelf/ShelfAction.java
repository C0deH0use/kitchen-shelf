package pl.codehouse.restaurant.shelf;

/**
 * Represents an action that can be performed on a shelf item.
 * This interface serves as a marker for different types of shelf actions,
 * such as creating a new item or updating an existing one.
 * Implementing classes should provide specific details for each action type.
 */
public interface ShelfAction {
    // This interface is intentionally left empty as a marker interface.
    // Specific action types will implement this interface and provide their own properties and methods.
}
